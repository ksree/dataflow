steps:
  - dataFrameName: transactions
    sql:
      SELECT *
      FROM transactions

output:
  - dataFrameName: transactions
    outputType: GCPBigQuery
    outputOptions:
      saveMode: Append
      dbTable: dataflow.transactions
  - dataFrameName: transactions
    outputType: JDBC
    outputOptions:
      saveMode: Append
      dbTable: transactions